{
  "name": "inled-apt-repo",
  "private": true,
  "scripts": {
    "dev": "python3 generate-web-index.py && npx serve public",
    "build": "python3 generate-web-index.py"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
